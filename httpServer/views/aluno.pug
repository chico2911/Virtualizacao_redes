extends layout



block content
    .w3-card-4
        header.w3-container.w3-teal
            h3 Ficha Aluno: #{aluno.nome}
        .w3-container
            table(class="w3-table-all")
                tr
                    th Número
                        td=aluno.id
                tr
                    th Nome
                        td=aluno.nome
                tr
                    th Instrumento
                        td= aluno.instrumento
                tr
                    th anoCurso
                        td= aluno.anoCurso
                tr
                    th curso
                        td= aluno.curso  
                tr
                    th dataNasc
                        td= aluno.dataNasc   
        if level==1
            form(action='/apagar/'+aluno.id method='GET')
                    input.w3-btn.w3-blue-grey(type='submit' value='Apagar')                                      
        footer(class="w3-container w3-teal")
                br
                a(href="/") Voltar 